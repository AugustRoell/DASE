"""Version number (major.minor.patch)"""

# `major` should be incremented only for big changes
# `minor` for changes that are not backward compatible
# `patch` is for all other changes that are backward compatible

__version__ = "1.0.0"
